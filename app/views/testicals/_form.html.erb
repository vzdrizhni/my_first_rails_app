<%= form_with(model: testical, local: true) do |form| %>
  <% if testical.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testical.errors.count, "error") %> prohibited this testical from being saved:</h2>

      <ul>
        <% testical.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :make %>
    <%= form.text_field :make %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
